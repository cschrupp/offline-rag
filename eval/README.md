# Evaluation Directory

- `datasets/` contains example schemas and, later, benchmark datasets.
- `baselines/` stores frozen baseline metric artifacts.
- `results/` stores machine-readable experiment results.
- `reports/` stores generated human-readable comparisons.

Do not commit private/proprietary evaluation data to a public repository.
